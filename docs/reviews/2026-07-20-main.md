# Review, main, 2026-07-20

**Reviewed by**: GPT-5.6 Terra (author on GPT-5)
**Scope**: 19 files, uncommitted
**Verdict**: Approve with nits

## Summary

The revised connected skeleton correctly protects the public read path: production requires `TRUST_PROXY_HOPS`, project technologies are published-only, bounded, and ordered by both required keys, and the web boundary distinguishes an unpublished profile from an unavailable API. The changed API and web unit suites and both workspace type checks pass. The remaining gap is executable database coverage for the new migration constraints.

## Minor

### 🟡 Database constraints have no database-backed regression coverage, `apps/api/prisma/migrations/20260720120000_add_portfolio_invariants/migration.sql:2`

**Problem**: The migration is not exercised by a migration or persistence test. The related unit tests verify mocked Prisma arguments and proxy parsing, but do not prove PostgreSQL rejects an invalid singleton key, negative order, or invalid current/completed state.
**Why it matters**: Tests are configured, and an unapplied or malformed check constraint would silently permit data that violates the model invariants.
**Suggested fix**: Run the migration against the isolated test database and add focused persistence assertions for each rejected state and valid boundary state.

## Strengths

- Production now fails before accepting traffic when `TRUST_PROXY_HOPS` is absent, while local and test defaults remain explicit and covered.
- Public project technologies are filtered to published skills, capped at 100, and ordered by `displayOrder` then `createdAt`, with focused unit coverage for the tie-breaker.
- The web API boundary validates successful payloads and routes the specified 404 to the dedicated Next.js not-found surface without compromising retry behavior for dependency failures.

## Test coverage

`pnpm.cmd --filter api test -- portfolio.service.spec.ts trust-proxy.spec.ts --runInBand` passed 9 tests; `pnpm.cmd --filter web test -- portfolio.test.ts` passed 6 tests; both API and web `typecheck` commands passed. With network access, the API E2E suite passed all 12 tests and the browser E2E suite passed all 9 tests. The changed unit and integration coverage exercises production proxy validation, public technology filtering/bounds/order, unpublished-profile handling, malformed API payloads, and visitor UI behavior; the migration itself remains unverified by direct persistence assertions against a migrated test database.
